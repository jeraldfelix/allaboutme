# allaboutme

This repository hosts a single-page IT professional portfolio.

## Requirements

The site uses no build tools, but `npx` may be needed for optional tooling.
If `npx` is not available on your system, install Node.js and npm:

```bash
sudo apt-get update
sudo apt-get install -y npm
```

This installs `npm` along with the `npx` command.
